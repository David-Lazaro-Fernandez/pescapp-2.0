allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}

android {
    defaultConfig {
        applicationId "com.example.pescapp"
        minSdkVersion 20                      
        targetSdkVersion 33
        versionCode 1
        versionName "1.0"
    }
}